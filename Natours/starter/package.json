{
  "name": "natours",
  "version": "1.0.0",
  "description": "Landing page for Natours",
  "main": "index.js",
  "scripts": {
    "devserver": "live-server",
    "css-compile": "node-sass sass -o css",
    "css-watch": "npm run css-compile && npm run css-compile -- -rw",
    "start": "npm run devserver & npm run css-watch"
  },
  "author": "Linda",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.12.0"
  }
}
